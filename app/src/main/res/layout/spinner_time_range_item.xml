<!-- spinner_time_range_item.xml -->
<layout xmlns:android="http://schemas.android.com/apk/res/android">
    <data>
        <variable name="timeRange" type="com.example.health.model.TimeRange"/>
    </data>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@{timeRange.displayName}"
        android:padding="8dp"
        android:textSize="14sp"/>
</layout>